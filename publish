PROJ=commentlogger
rm -rf src build dist flake8_multiline_equals.egg-info/
mkdir -p src/"$PROJ"

for fname in __init__.py checker.py ; do
    cp "$fname" src/"$PROJ"/
done

python -m build
python -m twine upload dist/*

rm -rf src build dist flake8_multiline_equals.egg-info/
